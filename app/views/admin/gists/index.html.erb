<div class="container mb-5">
  <h1><%= t('.header') %></h1>
  <table class="table table-striped">
    <thead>
      <tr>
        <th><%= t('.question') %></th>
        <th><%= t('.gist_link') %></th>
        <th><%= t('.user') %></th>
      </tr>
    </thead>
    <tbody>
      <%= render @gists %>
    </tbody>
  </table>
</div>
