<div class="container">
  <h1><%= t('.header')%></h1>

  <%= render 'form', badge: @badge %>
  <%= link_to t('.show'), admin_badge_url(@badge), class: 'btn btn-primary' %>
  <%= link_to t('.back'), admin_badges_url, class: 'btn btn-primary' %>
</div>
